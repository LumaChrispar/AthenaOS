{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Command Schema (CQRS)",
  "description": "Represents an imperative action requesting a service to do something.",
  "type": "object",
  "allOf": [
    { "$ref": "message.schema.json" }
  ],
  "properties": {
    "command_type": {
      "type": "string",
      "description": "The imperative action (e.g., 'WriteChapter', 'RunQACheck')"
    },
    "parameters": {
      "type": "object",
      "description": "Specific parameters required to execute the command"
    }
  },
  "required": ["command_type", "parameters"]
}
